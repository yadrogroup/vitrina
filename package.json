{
  "name": "vitrina",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "sync": "tsx scripts/sync.ts",
    "embed": "tsx scripts/embed.ts",
    "prepare-demo-images": "tsx scripts/prepare-demo-images.ts",
    "enrich": "tsx scripts/enrich.ts",
    "fetch-model": "tsx scripts/fetch-model.ts",
    "client:build": "npm run sync && npm run embed && npm run enrich && npm run build",
    "verify-theme": "tsx scripts/verify-theme.ts",
    "quality": "node scripts/quality-lighthouse.mjs",
    "relay:install": "npm install --prefix relay",
    "relay:dev": "npm --prefix relay start",
    "relay:test": "npm --prefix relay run test-order",
    "inference:install": "npm install --prefix inference",
    "inference:start": "npm --prefix inference start",
    "admin:install": "npm install --prefix admin",
    "admin:start": "npm --prefix admin start"
  },
  "dependencies": {
    "@fontsource-variable/onest": "^5.2.11",
    "@fontsource/golos-text": "^5.2.8",
    "@huggingface/transformers": "^4.2.0",
    "astro": "^6.4.7",
    "csv-parse": "^7.0.0",
    "fast-xml-parser": "^5.9.0"
  },
  "devDependencies": {
    "onnxruntime-node": "^1.26.0",
    "sharp": "0.35.1",
    "tsx": "^4.21.0"
  },
  "overrides": {
    "sharp": "0.35.1"
  }
}
